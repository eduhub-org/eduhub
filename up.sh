#!/bin/sh

# this is just a minimal script to bring everything up with docker-compose
# more of a reminder of what to do
# detach from the logs to not spam the console
# You can use docker logs <containername> instead
docker-compose up -d