/** Matches DB default for `Project.title` (see migrations). */
export const PROJECT_FALLBACK_TITLE = 'Neues Projekt';

/** Max length for `Project.tagline` (subtitle) in input fields. */
export const PROJECT_TAGLINE_MAX_LENGTH = 160;
