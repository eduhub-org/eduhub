import { CircularProgress } from "@material-ui/core";

const Loading = () => {
  return (
    <div className="place-items-center">
      <CircularProgress />
    </div>
  );
};
export default Loading;
