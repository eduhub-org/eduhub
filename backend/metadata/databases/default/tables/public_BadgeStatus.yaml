table:
  name: BadgeStatus
  schema: public
is_enum: true
array_relationships:
  - name: ProjectBadges
    using:
      foreign_key_constraint_on:
        column: status
        table:
          name: ProjectBadge
          schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - value
        - comment
      filter: {}
