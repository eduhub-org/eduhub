table:
  name: ProjectStatus
  schema: public
is_enum: true
array_relationships:
  - name: Projects
    using:
      foreign_key_constraint_on:
        column: status
        table:
          name: Project
          schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - value
        - comment
      filter: {}
