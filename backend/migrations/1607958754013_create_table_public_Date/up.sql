CREATE TABLE "public"."Date"("Id" serial NOT NULL, "Name" text NOT NULL, "Description" text NOT NULL, "Start" timestamptz NOT NULL, "End" timestamptz NOT NULL, "Location" text NOT NULL, "SurveyType" integer, "SurveyId" text, "CourseId" integer NOT NULL, PRIMARY KEY ("Id") , FOREIGN KEY ("CourseId") REFERENCES "public"."Course"("Id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("Id"));
