CREATE TABLE "public"."CertificateTemplateText" ("id" integer NOT NULL, "title" text NOT NULL, "html" text NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), "updated_at" timestamptz NOT NULL DEFAULT now(), PRIMARY KEY ("id") , UNIQUE ("id"), UNIQUE ("title"));COMMENT ON TABLE "public"."CertificateTemplateText" IS E'Certificate text templates is the basis for the textual content that is dynamically generated according to the course, user enrollment data, and certificate type.';
